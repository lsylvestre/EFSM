let rec fact(n) =
  if true then
    let rec aux(n,acc) =
      if n <= 0 then return acc
      if n > 0 then
        aux(n - 1, n * acc) 
    in
    aux(n,1)
in
rdy := '1';
let rec main(n) =
  (* [#] est la fonction de conversion de std_logic vers bool *)
  if #start then
    rdy := '0'; 
    fact(n)
  if not (#start) then main (n) 
in
main (dataa)
