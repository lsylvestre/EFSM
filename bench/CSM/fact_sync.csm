let rec fact(n) =
  if true then
    let rec aux(n,acc) =
      if n <= 0 then return acc
      if n > 0 then
        aux(n - 1, n * acc) 
    in
    aux(n,1)
in
rdy := '1';
let rec main(n) =
  if start = '1' then
    rdy := '0'; 
    fact(n)
  if not (start = '1') then main (n) 
in
main (dataa)
